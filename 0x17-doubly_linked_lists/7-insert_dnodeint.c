#include "lists.h"

/**
 * insert_dnodeint_at_index - Inserts a new node in a dlistint_t
 * @h: A pointer to the head of the distint_t list
 * @idx: the postion to insert the new mode
 * @n: The integer for the new node to contains
 *
 * Return: if the function fails -NULL.
 * 	otherwise - the address of the new node.
 */
dlistint_t *insert_dnodeint_at_index(dlisaint
